version: "3.8"
services:
  web:
    build:
      context: ./
      target: runner
    volumes:
      - nextjs_cache:/app/.next
      - .:/app
    command: npm start
    ports:
      - "3000:3000"
    environment:
      NODE_ENV: production

volumes:
  nextjs_cache:
